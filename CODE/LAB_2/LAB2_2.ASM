DATA 	SEGMENT
DATA1   DB 99H
DATA2	DB 99H
DATA3	DB ?,?
DATA    ENDS
CODE    SEGMENT
    	ASSUME CS:CODE,DS:DATA
START:	MOV AX,DATA
	MOV DS,AX
	MOV BL,DATA1[0]
	MOV CL,DATA2[0]
	MOV DX,0	
LOOP1:	CMP BL,0
	JZ SAVE
	CMP CL,0
	JZ SAVE
	MOV AL,DL
	ADD AL,BL
	DAA
	MOV DL,AL
	MOV AL,DH
	ADC AL,0
	DAA
	MOV DH,AL
	MOV AL,CL
	SUB AL,1
	DAS
	MOV CL,AL
	JMP LOOP1		
SAVE:	MOV DATA3[0],DL
	MOV DATA3[1],DH
	MOV AH,4CH
	INT 21H
CODE 	ENDS
	END START
